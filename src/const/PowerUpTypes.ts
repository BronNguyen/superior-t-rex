export default interface PowerUpType {
    name: string;
    imgTexture: string;
  }

  const shoe = {
    name: 'shoe',
    imgTexture: 'shoe',
  }

  const shield = {
    name: 'shield',
    imgTexture: 'shield',
  }

  export const powerUpTypes = [shoe, shield];
